[package]
name    = "system2json"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = { version = "=1.0.149", default-features = false, features = [ "std", "preserve_order" ] }

[dev-dependencies]
serde      = { version = "=1.0.228", default-features = false, features = [ "derive" ] }
